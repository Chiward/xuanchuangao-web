我将按照以下计划为您完成导航栏重构、历史记录迁移以及意见反馈功能的开发：

### 1. 数据库与存储初始化 (SQL)
*   创建 `feedback` 数据表：包含 `feedback_type`, `content`, `attachments` (数组存储文件路径), `contact`, `status` (默认 pending), `admin_reply` 等字段。
*   配置 RLS：确保用户只能查看/提交自己的反馈，管理员（后续）可查看所有。
*   配置 Storage Bucket：复用 `user_uploads` 或新建 `feedback_uploads` 用于存储反馈截图。

### 2. 导航栏与页面重构
*   **重构 SiteHeader**：将原有的简单 Header 改造为包含导航链接的完整导航栏（首页、历史记录、意见反馈）。
*   **创建历史记录页 (`app/history/page.tsx`)**：
    *   将首页底部的 `<HistoryList />` 迁移至此独立页面。
    *   移除首页底部的历史记录模块，保持首页专注“开始创作”。
*   **创建意见反馈页 (`app/feedback/page.tsx`)**：
    *   实现反馈表单 UI。

### 3. 开发意见反馈功能
*   **前端**：实现包含类型选择、内容输入、图片上传（支持预览/删除）、联系方式的表单。
*   **后端交互**：提交时将数据写入 `feedback` 表，图片上传至 Supabase Storage。
*   **反馈列表（用户端）**：在反馈页下方展示用户自己的历史反馈记录及处理状态。

### 4. 测试与验证
*   验证导航切换流畅性。
*   验证历史记录在新页面加载是否正常。
*   验证反馈提交流程（含图片上传）及数据入库完整性。

确认后，我将为您执行上述步骤。