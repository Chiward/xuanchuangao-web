已完成所有功能的重构与开发：

1. **导航栏重构与功能迁移**：

   * `components/site-header.tsx` 已更新，新增了 **首页**、**历史记录**、**意见反馈** 三个主要导航项。

   * 创建了 `app/history/page.tsx`，成功将 `HistoryList` 组件迁移至独立的 **历史记录** 页面。

   * 清理了 `app/page.tsx` (首页)，移除了原有的历史记录列表，使首页更简洁，专注于模板选择。

2. **意见反馈功能开发**：

   * 创建了 `feedback_feature_init.sql`，用于初始化 `feedback` 数据库表和 RLS 策略。

   * 创建了 `app/feedback/page.tsx`，实现了完整的反馈表单，包含：

     * **反馈类型**：下拉选择（功能异常、产品建议、其他）。

     * **反馈内容**：多行文本输入。

     * **联系方式**：可选输入。

     * **图片附件**：支持上传截图（自动存入 Supabase Storage）。

   * 表单提交后会自动保存至数据库，并给出成功提示。

3. **后续操作指引**：

   * 请务必在 **Supabase Dashboard -> SQL Editor** 中执行 `feedback_feature_init.sql` 脚本，以创建反馈表和存储策略。

   * 执行完 SQL 后，建议重启服务 (`npm run dev`) 以确保所有新路由生效。

   * 您可以访问 `/feedback` 页面测试反馈功能，或点击导航栏进行页面切换体验。

